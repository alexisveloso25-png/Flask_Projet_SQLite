<!DOCTYPE html>
<html>
<head>
    <title>Recherche Client</title>
</head>
<body>
    <h2>Recherche de fiche client par nom</h2>
    
    <form method="POST">
        <input type="text" name="nom_client" placeholder="Tapez un nom (ex: Dupont)" required>
        <button type="submit">Rechercher</button>
    </form>

    <hr>

    {% if data %}
        <h3>Résultats :</h3>
        <table border="1">
            <tr>
                <th>ID</th><th>Nom</th><th>Prénom</th>
            </tr>
            {% for client in data %}
            <tr>
                <td>{{ client[0] }}</td>
                <td>{{ client[1] }}</td>
                <td>{{ client[2] }}</td>
            </tr>
            {% endfor %}
        </table>
    {% elif request.method == 'POST' %}
        <p>Aucun client trouvé pour ce nom.</p>
    {% endif %}

    <br>
    <a href="{{ url_for('hello_world') }}">Retour à l'accueil</a>
</body>
</html>
